import { PackageManagerTabs } from '@theme'

# Vanilla JS / TS

Storybook for vanilla JavaScript / TypeScript & Rsbuild is a framework that makes it easy to develop and test UI components in isolation for applications built with Rsbuild.

:::info{title=Requirements}

- @rsbuild/core ≥ 1.0.1
- storybook ≥ 9.0.0

:::

## Getting started

Use with vanilla JavaScript / TypeScript in a existing Rsbuild project.

### Installation

To use vanilla JavaScript / TypeScript with Storybook, you need to install the `storybook-html-rsbuild` package.

<PackageManagerTabs command="install storybook-html-rsbuild -D" />

### Setup `.storybook/main.ts`

```js
import { StorybookConfig } from 'storybook-html-rsbuild'

const config: StorybookConfig = {
  framework: 'storybook-html-rsbuild',
  rsbuildFinal: (config) => {
    // Customize the final Rsbuild config here
    return config
  },
}

export default config
```

You're all set now. You could also check out the example in [sandboxes/vanilla-ts](https://github.com/rspack-contrib/storybook-rsbuild/blob/main/sandboxes/vanilla-ts) and use all other features listed in [Storybook site](https://storybook.js.org/docs).

## Options

Check out [types.ts](https://github.com/rspack-contrib/storybook-rsbuild/blob/main/packages/framework-html/src/types.ts).
